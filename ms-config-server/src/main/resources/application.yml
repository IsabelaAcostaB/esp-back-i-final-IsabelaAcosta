server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IsabelaAcostaB/esp-back-i-final-IsabelaAcosta-
          default-label: develop
          force-pull: true
          search-paths: configs